# ══════════════════════════════════════════════════════════════════
#  Skyreal — Nginx Configuration Template
#  Copy to: /etc/nginx/sites-available/skyreal.org
#  Then: sudo ln -s /etc/nginx/sites-available/skyreal.org /etc/nginx/sites-enabled/
#        sudo nginx -t && sudo systemctl reload nginx
# ══════════════════════════════════════════════════════════════════

server {
    listen 80;
    server_name skyreal.org www.skyreal.org;

    # Redirect HTTP → HTTPS (Certbot adds this automatically)
    # After running certbot, this block is replaced with the HTTPS config below.

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        application/json
        application/javascript
        application/xml+rss
        image/svg+xml;

    location / {
        proxy_pass         http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }

    # Cache Next.js static assets
    location /_next/static/ {
        proxy_pass http://localhost:3000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

# ── HTTPS (added by Certbot — for reference) ─────────────────────
# server {
#     listen 443 ssl http2;
#     server_name skyreal.org www.skyreal.org;
#
#     ssl_certificate     /etc/letsencrypt/live/skyreal.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/skyreal.org/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     ... (same location blocks as above)
# }
